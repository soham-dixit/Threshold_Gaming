$.fn.progressBar = function () {
  function d() {
    c.style.width = (window.scrollY / e) * 100 + "%";
  }
  var a = 0 < arguments.length && void 0 !== arguments[0] ? arguments[0] : {};
  a = {
    throttleTime: a.throttleLimit || 100,
    debounceTime: a.debounceTime || 200,
    progressBarHeight: a.progressBarHeight || 10,
    progressBarColor: a.progressBarColor || "#F1F1F1",
    progressBarValueColor: a.progressBarValueColor || "#A1A1A1",
    progressBarTopPosition: a.progressBarTopPosition || 0,
  };
  var f = $(this)[0],
    b = window.innerHeight,
    g = f.clientHeight,
    e = g > b ? g - b : 0;
  b = document.createElement("div");
  var c = document.createElement("div");
  b.value = 20;
  b.max = e;
  b.style.height = a.progressBarHeight + "px";
  b.style.backgroundColor = a.progressBarColor;
  b.style.position = "fixed";
  b.style.width = "100%";
  b.style.top = a.progressBarTopPosition + "px";
  b.style.left = 0;
  c.style.position = "absolute";
  c.style.width = "0";
  c.style.height = "100%";
  c.style.backgroundColor = a.progressBarValueColor;
  c.style.transition = "width 0.3s linear";
  f.appendChild(b);
  b.appendChild(c);
  document.addEventListener(
    "scroll",
    (function (a, b) {
      var c = !1;
      return function () {
        c ||
          (a(),
          (c = !0),
          setTimeout(function () {
            c = !1;
          }, b));
      };
    })(d, a.throttleLimit)
  );
  document.addEventListener(
    "scroll",
    (function (a, b) {
      var c;
      return function () {
        var d = this,
          e = arguments;
        clearTimeout(c);
        c = setTimeout(function () {
          a.apply(d, e);
        }, b);
      };
    })(d, a.debounceTime)
  );
};
$("article").progressBar();
